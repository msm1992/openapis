environments:
  - name: dev
    endpoints:
      production:
      sandbox:
  - name: misc_prod
    endpoints:
      production:
      sandbox:
  - name: production
    endpoints:
      production:
      sandbox:
  - name: test
    endpoints:
      production:
      sandbox:

deploy:
  import:
    update: true
    preserveProvider: true
